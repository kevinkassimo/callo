{
  "name": "callocall",
  "version": "0.0.2",
  "description": "Front-end API call wrapper for Callo",
  "main": "dist/index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "rm -rf dist/** && babel src --out-dir dist && jest tests/*.test.js",
    "build": "rm -rf dist/** && babel src --out-dir dist",
    "clean": "rm -rf dist"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kevinkassimo/callo.git"
  },
  "keywords": ["api", "web", "server"],
  "author": "Kevin Qian <kevinkassimo@gmail.com> (http://ksm.sh)",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-0": "^6.24.1",
    "callo": "0.0.2",
    "jest": "^23.1.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "isomorphic-fetch": "^2.2.1"
  }
}
