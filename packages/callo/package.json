{
  "name": "callo",
  "version": "0.0.1",
  "description": "Experiment with Minimal Stateful HTTP API",
  "main": "./dist/index.js",
  "scripts": {
    "test": "rm -rf dist/** && babel src --out-dir dist && jest tests/*.test.js",
    "build": "rm -rf dist/** && babel src --out-dir dist",
    "clean": "rm -rf dist"
  },
  "files": [
    "dist",
    "LICENSE"
  ],
  "keywords": [],
  "author": "Kevin Qian <kevinkassimo@gmail.com> (http://ksm.sh)",
  "license": "ISC",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "body-parser": "^1.18.3",
    "compression": "^1.7.2",
    "debug": "^3.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-0": "^6.24.1",
    "jest": "^23.1.0"
  }
}
